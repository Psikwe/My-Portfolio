import React from "react";

function Banner() {
  return (
    <div className="grid lg:grid-cols-2 ">
      <div>
        <p>
          If you need to use a one-off grid-template-columns value that doesn’t
          make sense to include in your theme, use square brackets to generate a
          property on the fly using any arbitrary value
        </p>
      </div>
      <div>
        <p>
          If you need to use a one-off grid-template-columns value that doesn’t
          make sense to include in your theme, use square brackets to generate a
          property on the fly using any arbitrary value
        </p>
      </div>
    </div>
  );
}

export default Banner;
